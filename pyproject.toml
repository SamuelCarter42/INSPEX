[build-system]
requires=["setuptools >= 77.0.3"]
build-backend="setuptools.build_meta"

[project]
version="0.1"
name="inspex"

[project.scripts]
inspex="inspex:fitting_gui"
instrument_choice="inspex:instrument_choice"
time_rng_select="inspex:time_rng_select"